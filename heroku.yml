setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: Dockerfile
release:
  command:
    - ./init_migrate.sh
  image: web
run:
  worker:
    command:
      - celery worker --app=mysearch.app -l info
    image: web
